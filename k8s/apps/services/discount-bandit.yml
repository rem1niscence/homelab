apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: discount-bandit
  namespace: argocd
  labels:
    type: service
spec:
  project: default
  source:
    repoURL: git@github.com:rem1niscence/homelab.git
    targetRevision: deploy
    path: k8s/services/discount-bandit
  destination:
    server: https://kubernetes.default.svc
    namespace: discount-bandit
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
