apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: glances
  namespace: argocd
  labels:
    type: service
spec:
  project: default
  source:
    repoURL: git@github.com:rem1niscence/homelab.git
    targetRevision: v2
    path: k8s/services/glances
  destination:
    server: https://kubernetes.default.svc
    namespace: glances
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
