apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: syncthing
  namespace: argocd
  labels:
    type: service
spec:
  project: default
  source:
    repoURL: git@github.com:rem1niscence/homelab.git
    targetRevision: v2
    path: k8s/services/syncthing
  destination:
    server: https://kubernetes.default.svc
    namespace: syncthing
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
