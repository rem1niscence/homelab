apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "homepage.name" . }}
  labels:
    {{- include "homepage.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "homepage.name" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "homepage.name" . }}
    namespace: {{ .Release.Namespace }}
