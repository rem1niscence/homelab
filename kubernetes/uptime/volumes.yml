apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: uptime
  name: config-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1G
  storageClassName: longhorn
